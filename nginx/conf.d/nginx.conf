 events {
    worker_connections 2048;  # Maximum number of simultaneous connections per worker process
}

http {
   upstream open_webui_app {
       server open-webui:8080;  # 'webui' is the service name in the Docker network
   }

#    upstream sonix_app {
#        server web_server:8000;  # 'web_server' is the service name in the Docker network
#    }

   # Use a map to check User-Agent headers
    map $http_user_agent $blocked_user_agent {
        default 0;
        "~*curl" 1;
        "~*wget" 1;
        # "~*bot" 1;
        # "~*spider" 1;
        # "~*crawl" 1;
        "~*Custom-AsyncHttpClient" 1;
        "~*Cpanel-HTTP-Client/1.0" 1;
        "Go-http-client/1.1" 1;
    }


##############################################
## REDIRECT TO HTTPS ##
##############################################
    server {
        listen 80;
        server_name 176.57.189.207 localhost verbaltranscript.com;
        client_max_body_size 4096M; 
        server_tokens off;
        access_log  /var/log/nginx/verbaltranscript.access.log;    
        error_log  /var/log/nginx/verbaltranscript.error.log;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        # location /favicon.ico {
        #     log_not_found off;
        #     access_log off;
        # }


        # # WebSocket endpoint
        # location /ws/ {
        #     proxy_pass http://open_webui_app;
        #     proxy_http_version 1.1;
        #     proxy_set_header Upgrade $http_upgrade;
        #     proxy_set_header Connection "upgrade";
        #     proxy_set_header Host $host; 
        #     proxy_read_timeout 86400;
        # }

        # location / {
        #     proxy_pass http://open_webui_app;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }
        
        return 301 https://www.aispeechpro.com$request_uri;
    }



    server {
        listen 443 ssl;
        server_name www.verbaltranscript.com;
        client_max_body_size 4096M; 
        server_tokens off;

        ssl_certificate /etc/letsencrypt/live/www.verbaltranscript.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/www.verbaltranscript.com/privkey.pem;
        include /etc/letsencrypt/options-ssl-nginx.conf;
        ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;


        add_header X-Content-Type-Options nosniff;
        add_header X-Frame-Options DENY;
        add_header X-XSS-Protection "1; mode=block";
    
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        # location /favicon.ico {
        #     log_not_found off;
        #     access_log off;
        # }
    
        # WebSocket endpoint
        location /ws/ {
            proxy_pass http://open_webui_app;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host; 
            proxy_read_timeout 86400;
        }

        location / {
            proxy_pass http://open_webui_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /static/ {
            alias /usr/src/static/;
            expires 1d;  # Cache for 30 days
            add_header Cache-Control "public, max-age=31536000";
        }
    

        # Reject requests that end with .php or .env
        location ~* \.(php|env|git)$ {
            return 403;  # Forbidden
        }

        # Reject requests that contain /wp/
        location /wp/ {
            return 403;  # Forbidden
        }

        location /geoserver/ {
            return 403;  # Forbidden
        } 

        # Block specific file extensions
        location ~* \.(php|env|git|ini|log|sql|conf|bak|old|sh|yml)$ {
            return 403;
        }

        # Block WordPress requests
        location ~* /(wp-admin|wp-login|wp-content|wp-includes)/ {
            return 403;
        }

        # Block HEAD and TRACE requests
        if ($request_method ~* (HEAD|TRACE)) {
            return 403;
        }

        # Block malicious User-Agents
        if ($blocked_user_agent) {
            return 403;
        }

        location ~* \.(git|env|config|dockerignore|gitignore|npmrc|yarnrc|bashrc|bash_profile|zshrc|DS_Store|thumbs\.db|desktop\.ini|gitmodules|composer\.json|package\.json|Pipfile|Pipfile\.lock|requirements\.txt|sqlite3|db|tmp|logs|backups|cache|error_log|access_log|secrets\.json|wp-admin) { 
            deny all; 
            return 403; 
        } 
    }


    # server {
    #     listen 80;
    #     server_name verbaltranscript.com;
    #     client_max_body_size 4096M; 
    #     server_tokens off;
    #     access_log  /var/log/nginx/verbaltranscript.access.log;    
    #     error_log  /var/log/nginx/verbaltranscript.error.log;

    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    #     location /favicon.ico {
    #         log_not_found off;
    #         access_log off;
    #     }
    #     return 301 https://www.verbaltranscript.com$request_uri;
    # }


    # ##############################################
    # ## REDIRECT to WWW  ##
    # ##############################################
    # server {
    #     listen 443 ssl;
    #     server_name aispeechpro.com;
    #     client_max_body_size 4096M; 
    #     server_tokens off; 

    #     ssl_certificate /etc/letsencrypt/live/www.aispeechpro.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/www.aispeechpro.com/privkey.pem;
    #     include /etc/letsencrypt/options-ssl-nginx.conf;
    #     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
        
    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    
    #     location /favicon.ico {
    #         log_not_found off;
    #         access_log off;
    #     }
    #     return 301 https://www.aispeechpro.com$request_uri;
    # }


    # server {
    #     listen 443 ssl;
    #     server_name verbaltranscript.com;
    #     client_max_body_size 4096M; 
    #     server_tokens off; 

    #     ssl_certificate /etc/letsencrypt/live/www.verbaltranscript.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/www.verbaltranscript.com/privkey.pem;
    #     include /etc/letsencrypt/options-ssl-nginx.conf;
    #     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
        
    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    
    #     location /favicon.ico {
    #         log_not_found off;
    #         access_log off;
    #     }
    #     return 301 https://www.verbaltranscript.com$request_uri;
    # }

    # ##############################################
    # ## Handle to WWW  HTTPS ##
    # ##############################################


    # server {
    #     listen 443 ssl;
    #     server_name www.aispeechpro.com;
    #     client_max_body_size 4096M; 
    #     server_tokens off;

    #     ssl_certificate /etc/letsencrypt/live/www.aispeechpro.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/www.aispeechpro.com/privkey.pem;
    #     include /etc/letsencrypt/options-ssl-nginx.conf;
    #     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;


    #     add_header X-Content-Type-Options nosniff;
    #     add_header X-Frame-Options DENY;
    #     add_header X-XSS-Protection "1; mode=block";
    
    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    #     location /favicon.ico {
    #         log_not_found off;
    #         access_log off;
    #     }
    
    #     location / {
    #         proxy_pass http://sonix_app;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }

    #     location /static/ {
    #         alias /usr/src/app/apps/static/;
    #         expires 1d;  # Cache for 30 days
    #         add_header Cache-Control "public, max-age=31536000";
    #     }
    #     location /static/admin/ {
    #         alias /usr/src/app/apps/static/admin/;
    #         expires 1d;  # Cache for 30 days
    #         add_header Cache-Control "public, max-age=31536000";
    #     }
        
    #     location /media {
    #         alias /usr/src/app/media; # Change to your own media directory here.
    #         access_log off;
    #     }



    #     # Reject requests that end with .php or .env
    #     location ~* \.(php|env|git)$ {
    #         return 403;  # Forbidden
    #     }

    #     # Reject requests that contain /wp/
    #     location /wp/ {
    #         return 403;  # Forbidden
    #     }

    #     location /geoserver/ {
    #         return 403;  # Forbidden
    #     } 

    #     # Block specific file extensions
    #     location ~* \.(php|env|git|ini|log|sql|conf|bak|old|sh|yml)$ {
    #         return 403;
    #     }

    #     # Block WordPress requests
    #     location ~* /(wp-admin|wp-login|wp-content|wp-includes)/ {
    #         return 403;
    #     }

    #     # Block HEAD and TRACE requests
    #     if ($request_method ~* (HEAD|TRACE)) {
    #         return 403;
    #     }

    #     # Block malicious User-Agents
    #     if ($blocked_user_agent) {
    #         return 403;
    #     }

    #     location ~* \.(git|env|config|dockerignore|gitignore|npmrc|yarnrc|bashrc|bash_profile|zshrc|DS_Store|thumbs\.db|desktop\.ini|gitmodules|composer\.json|package\.json|Pipfile|Pipfile\.lock|requirements\.txt|sqlite3|db|tmp|logs|backups|cache|error_log|access_log|secrets\.json|wp-admin) { 
    #         deny all; 
    #         return 403; 
    #     }
    
    # }


    # server {
    #     listen 443 ssl;
    #     server_name www.verbaltranscript.com;
    #     client_max_body_size 4096M; 
    #     server_tokens off;

    #     ssl_certificate /etc/letsencrypt/live/www.verbaltranscript.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/www.verbaltranscript.com/privkey.pem;
    #     include /etc/letsencrypt/options-ssl-nginx.conf;
    #     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;


    #     add_header X-Content-Type-Options nosniff;
    #     add_header X-Frame-Options DENY;
    #     add_header X-XSS-Protection "1; mode=block";
    
    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }
    #     location /favicon.ico {
    #         log_not_found off;
    #         access_log off;
    #     }
    
    #     # WebSocket endpoint
    #     location /ws/ {
    #         proxy_pass http://open_webui_app;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host; 
    #         proxy_read_timeout 86400;
    #     }

    #     location / {
    #         proxy_pass http://open_webui_app;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    

    #     # Reject requests that end with .php or .env
    #     location ~* \.(php|env|git)$ {
    #         return 403;  # Forbidden
    #     }

    #     # Reject requests that contain /wp/
    #     location /wp/ {
    #         return 403;  # Forbidden
    #     }

    #     location /geoserver/ {
    #         return 403;  # Forbidden
    #     } 

    #     # Block specific file extensions
    #     location ~* \.(php|env|git|ini|log|sql|conf|bak|old|sh|yml)$ {
    #         return 403;
    #     }

    #     # Block WordPress requests
    #     location ~* /(wp-admin|wp-login|wp-content|wp-includes)/ {
    #         return 403;
    #     }

    #     # Block HEAD and TRACE requests
    #     if ($request_method ~* (HEAD|TRACE)) {
    #         return 403;
    #     }

    #     # Block malicious User-Agents
    #     if ($blocked_user_agent) {
    #         return 403;
    #     }

    #     location ~* \.(git|env|config|dockerignore|gitignore|npmrc|yarnrc|bashrc|bash_profile|zshrc|DS_Store|thumbs\.db|desktop\.ini|gitmodules|composer\.json|package\.json|Pipfile|Pipfile\.lock|requirements\.txt|sqlite3|db|tmp|logs|backups|cache|error_log|access_log|secrets\.json|wp-admin) { 
    #         deny all; 
    #         return 403; 
    #     } 
    # }

}